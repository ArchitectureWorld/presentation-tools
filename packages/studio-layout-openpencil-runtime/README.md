# Studio Layout OpenPencil Runtime Boundary

This package contains the non-production boundary used by the isolated
Report Studio v0.2.0 OpenPencil runtime smoke. It validates isolation,
loopback capability URLs, exact dependency coordinates, real execution
evidence, and document lifecycle invariants. It does not register a DSH
profile or write a production document.
