# Studio Layout Engine Binding

Derived, replaceable mapping between Report Studio `layoutElementId` values and private canvas-engine node IDs.

The binding is operational data. It never belongs in Canonical Layout and never makes an engine document the project fact source.
